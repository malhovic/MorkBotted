services:
  morkbotted:
    build: .
    container_name: morkbotted
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DATA_DIR: /app/data
      DB_PATH: /app/data/morkbotted.db
    volumes:
      - ./data:/app/data
